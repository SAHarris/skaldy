---
---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SKALDY | Intel</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&family=Roboto+Mono:wght@400;600&display=swap" rel="stylesheet">
    <style>
        /* Base & Typography */
        body { 
            background-color: #050505;
            color: #e0e0e0; 
            font-family: 'Inter', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            min-height: 100vh; 
            margin: 0; 
            padding: 40px 20px;
        }

        .container { 
            width: 100%;
            max-width: 800px; 
            animation: fadeIn 1s ease-in;
        }

        /* Header & Footer */
        .intel-header, .intel-footer {
            width: 100%;
            max-width: 800px;
            text-align: center;
        }

        .intel-header {
            margin-bottom: 40px;
        }
        
        .return-link {
            font-family: 'Roboto Mono', monospace;
            color: #9d4edd;
            text-decoration: none;
            transition: color 0.3s ease, text-shadow 0.3s ease;
        }

        .return-link:hover {
            color: #fff;
            text-shadow: 0 0 10px rgba(157, 78, 221, 0.7);
        }

        .intel-footer { 
            margin-top: 80px; 
            font-size: 0.75rem; 
            color: #444; 
            letter-spacing: 0.05rem;
        }

        /* Markdown Content Styling */
        .prose {
            width: 100%;
        }

        .prose h1, .prose h2, .prose h3 {
            color: #ffffff;
            border-bottom: 1px solid #444;
            padding-bottom: 10px;
            margin-top: 40px;
            margin-bottom: 20px;
        }
        
        .prose h1 {
            font-size: 2.5rem;
            letter-spacing: 0.1rem;
        }
        
        .prose h2 {
            font-size: 1.8rem;
            color: #e0e0e0;
        }

        .prose p {
            line-height: 1.7;
            margin-bottom: 20px;
            color: #b0b0b0;
        }

        .prose a {
            color: #9d4edd;
            text-decoration: none;
            transition: text-shadow 0.3s ease;
        }

        .prose a:hover {
            text-shadow: 0 0 8px rgba(157, 78, 221, 0.6);
        }

        /* Code Blocks */
        .prose pre, .prose code {
            font-family: 'Roboto Mono', monospace;
        }
        
        .prose pre {
            background-color: #1a1a1a;
            border: 1px solid #333;
            padding: 20px;
            border-radius: 4px;
            overflow-x: auto;
            box-shadow: 0 4px 10px rgba(0,0,0,0.3);
        }

        .prose code {
            background-color: #2a2a2a;
            padding: 2px 5px;
            border-radius: 3px;
            color: #c792ea; /* Lighter purple for inline code */
        }
        
        .prose pre > code {
            background: none;
            padding: 0;
            color: inherit;
        }

        /* Blockquote */
        .prose blockquote {
            border-left: 3px solid #7b2cbf;
            margin-left: 0;
            padding-left: 20px;
            font-style: italic;
            color: #aaa;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(15px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
    <header class="intel-header">
        <a href="/intel" class="return-link">&larr; Return to Intel Directory</a>
    </header>

    <main class="container">
        <article class="prose">
            <slot />
        </article>
    </main>

    <footer class="intel-footer">
        WICHITA, KANSAS &nbsp;|&nbsp; &copy; 2026 SKALDY LLC
    </footer>
</body>
</html>
